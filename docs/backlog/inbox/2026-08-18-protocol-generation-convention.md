---
title: "Конвенция поколения протокола в realtime-доке"
description: Рецепт «v: z.literal(N) первым полем» и различение отказа по поколению от битой схемы через уже существующий Zod error в onRejected — доками, без нового механизма.
type: task
status: inbox
created: 2026-08-18
updated: 2026-08-18
---

# Конвенция поколения протокола

## Зачем

Любой распределённый потребитель приходит к полю поколения
(`v: z.literal(1)` первым полем), чтобы чужая запись опознавалась раньше, чем
интерпретировалась. Сегодня отказ по несовпадению поколения и битая схема
выглядят в `onRejected` одинаково, а лечатся по-разному (обнови соседа vs чини
баг).

Решение — **доками, не механизмом**: `RealtimeRejectedEvent` уже несёт Zod
error, а какое поле является «поколением» — знает только приложение; учить
фреймворк слову «version» значило бы вшить конвенцию в код. Отличить отказ по
поколению можно сегодня: первый issue с `path: ['v']` и кодом literal-мисматча.

## Результат

- Раздел realtime guide «Protocol generations»: почему literal-поле первым,
  рецепт различения в `onRejected` (по `error.issues[].path`), что делать при
  несовпадении (не чинить схему, а обновлять соседа).
- Явная фиксация границы: механизм различения в core не добавляем, пока минимум
  два потребителя не упрутся в недостаточность рецепта (тогда — отдельная
  задача с их фактами).

## План

- [ ] Написать раздел в `docs/guide/realtime.md` с рабочим сниппетом
      классификатора поверх `onRejected`.
- [ ] Проверить сниппет компиляцией/тестом (doc-код должен быть честным).
- [ ] llms regen через build; CHANGELOG не нужен (docs-only) — свериться с
      конвенцией репо.

## Acceptance

- [ ] Сниппет из гайда различает «поколение» и «битая схема» в реальном тесте
      или type-check прогоне.
- [ ] В доке зафиксировано, почему это конвенция, а не API.
