#!/bin/sh
# Classify every ref in this push once. Branch updates run the full repository
# gate; release tags run only deterministic local metadata preflight. A mixed
# push does both without repeating the expensive gate.
set -e
exec bun scripts/release-plan.ts pre-push
